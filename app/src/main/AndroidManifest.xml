<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_lemonade"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        tools:targetApi="31">

        <activity
            android:name=".Lemonade.Lemonade"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".TipTime.TipTime"
            android:exported="false"
            android:icon="@mipmap/ic_launcher_tip_time"
            android:label="@string/tip_time"/>
            <activity
                android:name=".Affirmations.MainClassAffirmations"
                android:exported="false" />

           <activity android:name=".DessertClicker.MainClassDessertClicker"
               android:exported="true"
               android:icon="@mipmap/ic_dessert_clicker"
               android:label="@string/app_name_dessertClicker"
               android:roundIcon="@mipmap/ic_dessert_clicker_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>